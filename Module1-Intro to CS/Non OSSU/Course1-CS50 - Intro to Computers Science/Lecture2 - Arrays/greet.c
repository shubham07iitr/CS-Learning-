#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(int argc, string argv[]) //this just means that command line will take arguments of an array named argv and countargc
{
    printf("hello, %s\n", argv[2]);
}